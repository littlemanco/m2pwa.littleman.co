---
# Generated by Boilr at 
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "nginx"
    role: "webserver"
  name: "nginx-deployment"
  namespace: "m2pwa-littleman-co"
spec:
  replicas: 2
  selector:
    matchLabels:
      application: "nginx"
      role: "webserver"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "nginx"
        role: "webserver"
    spec:
      containers:
      - name: "nginx"
        image: "m2pwa-littleman-co-frontend-webserver:{{ APP_VERSION }}"
        imagePullPolicy: "IfNotPresent"
        ports:
        - containerPort: 80
          protocol: TCP
        livenessProbe:
          httpGet:
            # Path to probe; should be cheap, but representative of typical behavior
            path: /
            port: 443
            httpHeaders:
              - name: Host
                value: m2pwa.littleman.co
          initialDelaySeconds: 5
          timeoutSeconds: 1
        readinessProbe:
          httpGet:
            # Path to probe; akin to a status check. 
            path: /status.html
            port: 443
            httpHeaders:
              - name: Host
                value: m2pwa.littleman.co
          timeoutSeconds: 1
      restartPolicy: "Always"
      securityContext: {} 
