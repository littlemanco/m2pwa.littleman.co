# Generated by Boilr at 
apiVersion: "extensions/v1beta1"
kind: "Deployment"
metadata:
  labels:
    application: "letsencrypt"
    role: "webserver"
  name: "letsencrypt-deployment"
  namespace: "m2pwa-littleman-co"
spec:
  replicas: 1
  selector:
    matchLabels:
      application: "letsencrypt"
      role: "webserver"
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 1
    type: "RollingUpdate"
  template:
    metadata:
      labels:
        application: "letsencrypt"
        role: "webserver"
    spec:
      containers:
      - name: "letsencrypt"
        env:
        - name: "EMAIL"
          value: "hello@andrewhowden.com"
        - name: "DOMAINS"
          value: "m2pwa.littleman.co" # todo: Add the api.m2pwa.littleman.co endpoint to this at some point.
        - name: "DEPLOYMENTS"
          value: "frontend-webserver" # Todo: Add backend webserver to this at some point.
        - name: "SECRET_NAME"
          value: "webserver-etc-tls"
        - name: "NAMESPACE"
          value: "m2pwa-littleman-co"
        image: "ployst/letsencrypt:latest" 
        imagePullPolicy: "Always"
        ports:
        - containerPort: 80
          protocol: TCP
      restartPolicy: "Always"
      securityContext: {} 
