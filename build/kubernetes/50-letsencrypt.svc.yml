# Generated by boilr at 
kind: "Service"
apiVersion: "v1"
metadata:
  name: "letsencrypt"
  namespace: "m2pwa-littleman-co"
spec:
  selector:
    application: "letsencrypt"
    role: "webserver"
  ports:
    - protocol: "TCP"
      name: "http"
      port: 80
    # - protocol: "TCP"
    #   name: "another-http"
    #   port: 30http
  type: "ClusterIP"
